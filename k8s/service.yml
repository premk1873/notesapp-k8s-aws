apiVersion: v1
kind: Service
metadata:
  name: cloudnotes-service
  namespace: cloudnotes
spec:
  type: NodePort
  selector:
    app: cloudnotes
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080